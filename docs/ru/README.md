# RusTorch 🚀

[![Crates.io](https://img.shields.io/crates/v/rustorch)](https://crates.io/crates/rustorch)
[![Documentation](https://docs.rs/rustorch/badge.svg)](https://docs.rs/rustorch)
[![License](https://img.shields.io/badge/license-MIT%2FApache--2.0-blue.svg)](https://github.com/JunSuzukiJapan/rustorch)
[![Tests](https://img.shields.io/badge/tests-968%20passing-brightgreen.svg)](#testing)
[![Build](https://img.shields.io/badge/build-passing-brightgreen.svg)](#testing)

**Готовая к продакшену библиотека глубокого обучения на Rust с API как в PyTorch, ускорением GPU и производительностью корпоративного уровня**

RusTorch - это полнофункциональная библиотека глубокого обучения, использующая безопасность и производительность Rust, предоставляющая всеобъемлющие тензорные операции, автоматическое дифференцирование, слои нейронных сетей, архитектуры трансформеров, многобэкэндное ускорение GPU (CUDA/Metal/OpenCL), продвинутые SIMD-оптимизации, управление памятью корпоративного уровня, валидацию данных и обеспечение качества, а также всеобъемлющие системы отладки и логирования.

## ✨ Возможности

- 🔥 **Всеобъемлющие тензорные операции**: Математические операции, broadcasting, индексирование и статистика
- 🤖 **Архитектура Transformer**: Полная реализация трансформера с multi-head attention
- 🧮 **Разложение матриц**: SVD, QR, разложение по собственным значениям с совместимостью с PyTorch
- 🧠 **Автоматическое дифференцирование**: Граф вычислений на основе ленты для вычисления градиентов
- 🚀 **Динамический движок выполнения**: JIT-компиляция и оптимизация времени выполнения
- 🏗️ **Слои нейронных сетей**: Linear, Conv1d/2d/3d, ConvTranspose, RNN/LSTM/GRU, BatchNorm, Dropout и многое другое
- ⚡ **Кроссплатформенные оптимизации**: SIMD (AVX2/SSE/NEON), платформо-специфичные и аппаратно-осознанные оптимизации
- 🎮 **Интеграция с GPU**: Поддержка CUDA/Metal/OpenCL с автоматическим выбором устройства
- 🌐 **Поддержка WebAssembly**: Полный ML в браузере со слоями нейронных сетей, компьютерным зрением и inference в реальном времени
- 🎮 **Интеграция WebGPU**: GPU-ускорение, оптимизированное для Chrome, с fallback на CPU для кроссбраузерной совместимости
- 📁 **Поддержка форматов моделей**: Safetensors, ONNX inference, совместимость с PyTorch state dict
- ✅ **Готовность к продакшену**: Пройдено 968 тестов, унифицированная система обработки ошибок
- 📐 **Расширенные математические функции**: Полный набор математических функций (exp, ln, sin, cos, tan, sqrt, abs, pow)
- 🔧 **Продвинутые перегрузки операторов**: Полная поддержка операторов для тензоров со скалярными операциями и присваиваниями на месте
- 📈 **Продвинутые оптимизаторы**: SGD, Adam, AdamW, RMSprop, AdaGrad с планировщиками скорости обучения
- 🔍 **Валидация данных и обеспечение качества**: Статистический анализ, обнаружение аномалий, проверка согласованности, мониторинг в реальном времени
- 🐛 **Всеобъемлющая отладка и логирование**: Структурированное логирование, профилирование производительности, отслеживание памяти, автоматические оповещения

## 🚀 Быстрый старт

**📓 Для полного руководства по настройке Jupyter см. [README_JUPYTER.md](../../README_JUPYTER.md)**

### Демо Python Jupyter Lab

📓 **[Полное руководство по настройке Jupyter](../../README_JUPYTER.md)** | **[Руководство по Jupyter](jupyter-guide.md)**

#### Стандартная демо на CPU
Запустить RusTorch с Jupyter Lab одной командой:

```bash
./start_jupyter.sh
```

#### Демо с ускорением WebGPU
Запустить RusTorch с поддержкой WebGPU для GPU-ускорения в браузере:

```bash
./start_jupyter_webgpu.sh
```

### Базовая установка

```bash
cargo add rustorch
```

### Простой пример

```rust
use rustorch::{
    tensor::Tensor,
    nn::{Linear, Module},
    optim::SGD,
};

fn main() -> Result<(), Box<dyn std::error::Error>> {
    // Создать тензор
    let x = Tensor::<f32>::new(vec![1.0, 2.0, 3.0, 4.0], vec![2, 2])?;
    let y = Tensor::<f32>::new(vec![0.0, 1.0], vec![2, 1])?;
    
    // Создать модель
    let mut model = Linear::<f32>::new(2, 1)?;
    let mut optimizer = SGD::new(model.parameters(), 0.01)?;
    
    // Обучение
    for epoch in 0..100 {
        // Прямой проход
        let pred = model.forward(&x)?;
        let loss = (pred - &y)?.pow(2.0)?.mean()?;
        
        // Обратный проход
        loss.backward()?;
        optimizer.step()?;
        optimizer.zero_grad()?;
        
        if epoch % 10 == 0 {
            println!("Эпоха {}: Потери = {:.4}", epoch, loss.data()[0]);
        }
    }
    
    Ok(())
}
```

## 📋 Требования

- **Rust**: Версия 1.75.0 или выше
- **Платформы**: Windows, macOS, Linux
- **Опционально**: CUDA 12.0+, OpenCL 2.0+, Metal (для GPU-ускорения)

## 🎯 Варианты использования

### 🏭 Готовность к корпоративному использованию
- ✅ **Пройдено 968 тестов**: Всеобъемлющий тестовый набор обеспечивает надёжность
- 🔒 **Безопасность памяти**: Rust предотвращает segfault'ы и утечки памяти
- 📊 **Профилирование производительности**: Оптимизировано для продакшн-нагрузок
- 🌍 **Кроссплатформенность**: Работает на Windows, macOS, Linux

### 🧠 Машинное обучение
- 🔥 **Модели Transformer**: Современный NLP с multi-head attention
- 🖼️ **Компьютерное зрение**: CNN, ResNet, классификация изображений
- 📊 **Анализ данных**: Статистика, обнаружение аномалий, временные ряды
- 🎯 **Обучение с подкреплением**: Policy gradients, Q-learning

### ⚡ Высокопроизводительные вычисления
- 🚀 **SIMD-оптимизации**: Векторизация AVX2/SSE/NEON
- 🎮 **Мульти-GPU**: Параллелизм CUDA/Metal/OpenCL
- 🧵 **Многопоточность**: Параллельная обработка на основе Rayon
- 💾 **Оптимизация памяти**: Эффективный пулинг и кеширование памяти

### 🌐 Веб и Edge развёртывание
- 📱 **WebAssembly**: Полный ML-pipeline в браузере
- ⚡ **WebGPU**: GPU-ускорение, оптимизированное для Chrome
- 🔧 **Edge-вычисления**: Встраиваемые системы и IoT
- 🌍 **Платформонезависимость**: Одна кодовая база, работает везде

## 🏗️ Архитектура

RusTorch следует модульной архитектуре с четкими обязанностями:

```
rustorch/
├── tensor/          # Основные тензорные операции
├── autograd/        # Автоматическое дифференцирование
├── nn/              # Слои нейронных сетей
├── optim/           # Алгоритмы оптимизации
├── data/            # Загрузка и предварительная обработка данных
├── gpu/             # GPU-бэкэнд (CUDA/Metal/OpenCL)
├── wasm/            # Привязки WebAssembly
└── examples/        # Примеры и демо
```

## 🔧 Функции

### Тензорные операции
```rust
let a = Tensor::<f32>::ones(vec![3, 3])?;
let b = Tensor::<f32>::eye(3)?;
let c = a.matmul(&b)?;  // Умножение матриц
let d = c.transpose(0, 1)?;  // Транспонирование
```

### GPU-ускорение
```rust
#[cfg(feature = "cuda")]
{
    let device = Device::cuda(0)?;
    let tensor = Tensor::<f32>::ones(vec![1000, 1000])?.to_device(&device)?;
    let result = tensor.matmul(&tensor)?;  // GPU-ускоренное
}
```

### Архитектура Transformer
```rust
use rustorch::nn::transformer::TransformerEncoder;

let encoder = TransformerEncoder::<f32>::new(
    512,    // d_model
    8,      // num_heads
    2048,   // d_ff
    6,      // num_layers
    0.1,    // dropout
)?;
```

## 📚 Документация

- 📖 **[Полная документация API](https://docs.rs/rustorch)**
- 📓 **[Руководство по настройке Jupyter](../../README_JUPYTER.md)**
- 🎯 **[Примеры](../../examples/)**
- 🧪 **[Тесты](../../tests/)**

## 🤝 Внесение вклада

Вклады приветствуются! См. [CONTRIBUTING.md](../../CONTRIBUTING.md) для деталей.

## 📄 Лицензия

Этот проект лицензируется под лицензией MIT ИЛИ Apache-2.0 - см. файлы [LICENSE](../../LICENSE) для подробностей.

## 🙏 Благодарности

- Вдохновлено PyTorch для дизайна API
- Сообщество Rust за исключительные инструменты
- Candle за вдохновение в Rust ML
- Все участники и тестировщики

---

**🚀 Готовы начать? Посмотрите нашу [демонстрацию Jupyter](../../README_JUPYTER.md)!**