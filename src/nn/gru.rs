//! Gated Recurrent Unit (GRU) layers implementation
//! Gated Recurrent Unit（GRU）レイヤーの実装

// Re-export from parent module for backward compatibility
pub use crate::nn::gru_cell::GRUCell;
pub use crate::nn::gru_layer::GRU;
